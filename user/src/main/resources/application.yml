server:
  port: 8080
spring:
    application:
      name: user
    datasource:
      url: jdbc:postgresql://localhost:5432/customer
      username: postgres
      password: 1729
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true